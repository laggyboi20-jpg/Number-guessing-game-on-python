import random

print('welcome to number guessing game!')

the_number = random.randint(1, 50)
max_tries = 5
count = 0
guessed_correctly = False

while count < max_tries:
    try:
        user_guess = int(input(f'please guess a number between 1 and 50: (attempt {count + 1}/{max_tries}): '))
    except ValueError:
        print('invalid input. please enter a whole number')
        continue

    count += 1

    if user_guess == the_number:
        print(f'you guessed it!!, the number was {the_number}, and it only took you{count}tries!')
        guessed_correctly = True
        break
    elif user_guess > the_number:
        print('too high try again')

    else:
        print('too low try again')

if not guessed_correctly:
    print(f'sorry, you ran out of tries the number was {the_number}')

    input('press any key to continue')